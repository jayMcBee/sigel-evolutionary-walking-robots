# Phase V5 RESULT -- our Qt 6 build against the 1.3 binary, 2026-08-27.
# Their half captured by gdb on the x86 box; ours from `sigel_eval -selfcheck`'s
# sibling probe, SIGEL_MDH=1. Full method in PORTING.md Phase V.
#
# VERDICT: PASS. No high-bit disagreement in any field of any joint.
#
#   twoBasesSimpleFitness1   EXACT, bit for bit, all four fields
#   octopusSimpleFitness     9/9 joints agree; alpha BIT-IDENTICAL on all 9;
#                            THREE joints exact in all four fields
#                            (thirdLegJoint2, thirdLegJoint3, firstLegJoint2);
#                            five within 1-5 ulp; one near-zero residual
#
# ROWS ARE MATCHED BY CALL ORDER. Both sides emit in call order natively, and
# V1 gates that order independently, so it is a key something else already
# checks.
#
# Rows are keyed by CALL ORDER, which V1 gates independently.
#
# The first table used a greedy minimum-total-ulp assignment, described at the
# time as "by alpha, which is unique per joint". alpha is not unique -- 6 of
# the 9 share 4012d97c7f3321d2. That method swapped secondLegJoint3 with
# thirdLegJoint3 and spread two exact rows onto their neighbours, reporting
# one exact joint where there are three.
#
# NEVER key reference data by minimising the difference under test.
#
# ulp distance per field, ours vs 1.3, in call order:
#
#   joint                 a  alpha    d  theta
#   secondLegJoint1       3      0    0      1
#   secondLegJoint2       0      0    ~      0    <- near-zero residual, see below
#   secondLegJoint3       1      0    2      2
#   thirdLegJoint1        5      0    1      0
#   thirdLegJoint2        0      0    0      0    EXACT
#   thirdLegJoint3        0      0    0      0    EXACT
#   firstLegJoint1        0      0    0      1
#   firstLegJoint2        0      0    0      0    EXACT
#   firstLegJoint3        0      0    0      2
#
# CORRECTION TO THE FIRST TABLE, confirmed by the x86 side against its raw
# capture. Two attributions swap once matched by call order:
#   secondLegJoint3  call order [1,0,2,2]   first reported [1,0,0,0]
#   thirdLegJoint3   call order [0,0,0,0]   first reported [0,0,2,2]
# The near-zero residual is on ONE joint, not two. And THREE joints are exact,
# not one -- the mis-assignment had spread thirdLegJoint3's and firstLegJoint2's
# rows onto neighbours. The corrected result is stronger than either side
# first stated.
#
# THE NEAR-ZERO RESIDUAL is not an ulp difference and counting it as one is
# meaningless. secondLegJoint2's d should be zero:
#   ours 0000000000000000 = 0        1.3  3ca6a09e667f3bcd = 1.570092e-16
# The 2003 build is the one carrying the crumb, not ours -- an x87 80-bit
# intermediate failing to round to zero the way an IEEE double does. That is
# the direction predicted in advance, which is why it reads as confirmation
# rather than as a defect.
#
# Largest real disagreement: thirdLegJoint1's a, 5 ulp, relative 8.7e-16.
#
# STILL OPEN on their side, reported as open rather than glossed:
#   applyForce      breakpoint armed, zero hits in a session where the MDH
#                   breakpoint fired 18 times, so the harness was live. Their
#                   untested guess is that the standalone visualiser steps
#                   physics without running the evolved program. NOT verified.
#   senseJoint1/2   not attempted; they return in st(0), so a stack read does
#                   not work and a finish-style capture is needed.
#   walker          36 expected; same recipe, not run.

================================================================================
V5 SENSOR AND FORCE PATHS -- CAPTURED 2026-09-08, MOST OF IT RE-DERIVED
================================================================================
Sent by the sigel-x86 session while V2 was closing, so that the V5 step does
not re-run the machine for what is already measured.

FIRST SENT FROM NOTES, THEN RE-READ OUT OF THE BINARY THE SAME DAY. The first
message quoted a file; asked to treat it as received rather than established,
the oracle re-derived it from vendor/kdesigel/sigel_slave and sent the evidence
below. A number measured once and quoted since is a citation, not a
measurement -- that applies to both ends of this channel.

TWO LINES ARE STILL RECEIVED, NOT ESTABLISHED, and they are marked where they
appear: the dead-code call graph, and the moveDrive range with its arithmetic.
Ask the oracle to run those down before relying on either.

DEAD CODE -- DO NOT PROBE.  *** RECEIVED, NOT RE-DERIVED ***
    senseJoint1 and senseJoint2 are reached only from
    SIG_DynaMoSimulationQueries::sense. Every shipped experiment sets
    SIMULATIONLIBRARY 1 (DynaMechs), so that interface is never constructed.
    The oracle lost time there once already.
    applyForce belongs to the DynaMo backend and never fires either.
    THE SIMULATIONLIBRARY 1 HALF HOLDS and is checkable in a second. The
    "no other caller exists" half is a call-graph claim nobody has re-derived,
    and it is the half that would bite. Do not treat it as established.

THE LIVE SENSOR PATH -- re-derived from the binary
    Enclosing symbol, from objdump:
      sense__CQ216SIGEL_Simulation30SIG_DynaMechsSimulationQueriesiRt7QVector1ZQ216SIGEL_Simulation12SIG_Register
    which settles the address range and the function identity together.
    0x080b1b8c..0x080b2580.

    The eight constants are a CONTIGUOUS .rodata table, and there are exactly
    eight distinct fldl targets in that range, 8 bytes apart:

      addr        raw (BE hex)        value
      0x081ec308  4000000000000000    2.0
      0x081ec310  ffefffffffffffff    -1.7976931348623157e+308
      0x081ec318  7fefffffffffffff     1.7976931348623157e+308
      0x081ec320  404ca5dc1af05a77    57.29577957855229
      0x081ec328  4076800000000000    360.0
      0x081ec330  4056800000000000    90.0
      0x081ec338  c056800000000000    -90.0
      0x081ec340  4066800000000000    180.0

THE RADIAN FACTOR IS NOT 180/pi. STRONGEST DO-NOT-FIX ITEM IN THIS FILE.
    180/3.14159265 = 57.29577957855229   bits 404ca5dc1af05a77
    180/pi         = 57.29577951308232   bits 404ca5dc1a63c1f8
    relative error   1.1426665202582356e-09

    THE DECISIVE MEASUREMENT IS AN ABSENCE. A byte search over both shipped
    binaries:
      sigel        3254451 bytes   truncated: 0 hits   true 180/pi: 0 hits
      sigel_slave  3052243 bytes   truncated: 1 hit at file offset 0x1a4320
                                   true 180/pi: 0 hits
    The truncated form occurs exactly once. The correct constant occurs
    NOWHERE in either image. That is stronger than "1.3 uses a truncated pi".
    File offset 0x1a4320 maps to vaddr 0x081ec320: the first LOAD is
    0x000000 -> 0x08048000, and .rodata is 0x081c9c20 + 0x616ab.

    3.14159265 is exactly what a reader tidies into M_PI. Doing so shifts every
    joint-sensor reading by ~1.1e-9 -- invisible at six digits, fed straight
    into a chaotic simulation, and the evolved programs in the shipped
    experiments were selected against sensor values carrying that error.

MULTIPLY BEFORE DIVIDE, disassembled
     80b1f1a: fldl   -0xf0(%ebp)
     80b1f20: fldl   0x81ec320          <- the truncated factor
     80b1f26: fmulp  %st,%st(1)         <- multiply
     80b1f28: fstpl  -0xf0(%ebp)        <- 64-bit store: this is where it
                                           overflows
     80b1f2e: fldl   -0xf0(%ebp)
     80b1f34: fdivl  -0xe8(%ebp)        <- divide, after
    The factor is loaded three times in the range: 0x80b1f20, 0x80b213b,
    0x80b2156.

THE +-DBL_MAX PAIR IS AN UNBOUNDED-JOINT SENTINEL, WITH A LATENT 1.3 BUG
    %g writes DBL_MAX as 1.79769e+308, which reads back as 7feffffc57ca82ae, so
    the == test fails after one round trip. posRange then overflows to +inf on
    the store, inf/inf gives NaN, and static_cast<int>(NaN) is undefined
    behaviour reached from a model file. Multiply-before-divide is what makes
    it UB; SIGEL 1.0 divided first and got a finite 0.
    LATENT ONLY: no shipped .exp or .rrb contains that literal, and all 61
    RotationalJoint records use finite limits.
    TESTING TRAP: q=DBL_MAX hides the difference. Use a realistic joint angle.

THE LIVE FORCE PATH  *** RECEIVED, NOT RE-DERIVED ***
    SIG_DynaMechsCommandInterface::moveDrive, 0x080b787c..0x080b8094.
    Computes the drive value inline from SIG_Register getValue() and getSize(),
    calling pow(2.0, size) twice with exponent getSize()-1, giving the signed
    range -2^(n-1) .. 2^(n-1)-1.
